#include "main.h"
/**
 *mul- multiplies two integers
 *@a: variable to be computed
 *@b: second variable to be computed
 *Return: Always 0 (Success)
 */
int mul(int a, int b)
{
	int multiply = a * b;

	return (multiply);
}


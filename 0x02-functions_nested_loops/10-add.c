#include "main.h"
/**
 *add - calculates sum of two integers
 *@a: first variable to be computed
 *@b: second variable to be computed
 *Return: Returns sum
 */
int add(int a, int b)
{
	int add = a + b;

	return (add);
}

#include <stdio.h>
/**
 * main - A program that outputs a text followed by a new line using printf
 *
 * Return: Always 0 (Sucess)
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
